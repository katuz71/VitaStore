# ✅ Чеклист перед деплоем в продакшен

## 🔍 Проверка конфигурации

### 1. ✅ .env файл создан
Если файл уже создан, проверьте что в нем есть:

```bash
# Обязательные переменные:
ENVIRONMENT=production
NOVA_POSHTA_API_KEY=ваш_ключ_здесь
ALLOWED_ORIGINS=https://yourdomain.com

# Опциональные (но рекомендуемые):
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
MONOBANK_API_TOKEN=ваш_токен
OPENAI_API_KEY=ваш_ключ
```

### 2. ⚠️ Обновить PROD_API_URL в app/config/api.ts

**ВАЖНО:** Замените `'https://yourdomain.com'` на ваш реальный домен!

Откройте `app/config/api.ts` и найдите строку 18:
```typescript
const PROD_API_URL = 'https://yourdomain.com'; // ЗАМЕНИТЕ НА ВАШ ДОМЕН
```

Замените на:
```typescript
const PROD_API_URL = 'https://ваш-реальный-домен.com';
```

### 3. ✅ Установить зависимости

```bash
# Backend
pip install -r requirements.txt

# Frontend (если еще не установлены)
npm install
```

### 4. ✅ Проверить что сервер запускается

```bash
python main.py
```

Должен запуститься без ошибок на порту 8001.

---

## 🚀 Для продакшена (если деплоите на сервер)

### 5. Настроить домен и SSL

- [ ] Купить/настроить домен
- [ ] Настроить DNS записи
- [ ] Установить SSL сертификат (Let's Encrypt)

### 6. Настроить сервер

- [ ] Установить Python 3.11+
- [ ] Установить все зависимости из `requirements.txt`
- [ ] Настроить Nginx (опционально, но рекомендуется)
- [ ] Настроить firewall (открыть порт 8001 или 80/443)

### 7. Собрать приложение

```bash
# Android
npx expo build:android

# iOS
npx expo build:ios
```

---

## 📝 Быстрая проверка

Запустите эти команды для проверки:

```bash
# 1. Проверить что .env существует
ls -la .env

# 2. Проверить что зависимости установлены
pip list | grep fastapi
npm list expo

# 3. Проверить что сервер запускается
python main.py
# (должен показать "Server is running" без ошибок)
```

---

## ⚠️ Критично перед запуском

1. **PROD_API_URL** - обязательно замените на ваш домен!
2. **ALLOWED_ORIGINS** - укажите ваш домен в .env
3. **NOVA_POSHTA_API_KEY** - должен быть в .env
4. **ENVIRONMENT=production** - установите в .env для продакшена

---

## ✅ Готово!

После выполнения всех пунктов проект готов к продакшену.



